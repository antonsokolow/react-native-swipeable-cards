import React from "react";
import { View, Text } from "react-native";

import * as Styles from "./FavoritesStyles";

const Favorites = () => {
    return (
        <Styles.Container>
            <Styles.Title>Favorites</Styles.Title>
        </Styles.Container>
    );
};

export default Favorites;
